<div class="row">
{{ form_start(form) }}
    <div class="col-sm-3 col-md-6 col-lg-8 border border-2 border-primary rounded-2 p-3">
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
</div>
